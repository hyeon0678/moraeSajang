<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd"
>

<mapper namespace="kr.co.morae.mypage.dao.MypageDao">

	<select id="myInfo"
		resultType="kr.co.morae.mypage.dto.MypageDto">
		SELECT u.userId ,u.nickname ,u.name ,u.email ,u.reg_date ,
		CONCAT(ua.streetAddress ,ua.detailAddress) as addr
		FROM user u join
		userAddress ua group by u.userId HAVING u.userId = #{param1}
	</select>

	<select id="totalPoint" resultType="int">
		SELECT SUM(total) FROM
		(SELECT SUM(pointMonney) as total FROM pointHistory WHERE userId =
		#{param1}
		UNION ALL
		SELECT SUM(chargePrice) as total FROM chargeHistory
		WHERE userId = #{param1}) as result
	</select>

	<select id="review" resultType="map">
		SELECT
		COUNT(CASE WHEN reviewNo = 1 then reviewNo end) as high
		,COUNT(CASE WHEN reviewNo = 2 then reviewNo end) as 'mid'
		,COUNT(CASE WHEN reviewNo = 3 then reviewNo end) as low
		FROM userReview WHERE
		userId = #{param1}
	</select>

	<update id="alarmSet">
		UPDATE userAuth SET authNo = #{param1} WHERE userId =
		#{param2}
	</update>

</mapper>